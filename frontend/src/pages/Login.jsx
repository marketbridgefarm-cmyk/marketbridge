import React,{useState}from'react';import{Link,useNavigate}from'react-router-dom';import{useAuth}from'../context/AuthContext.jsx';
export default function Login(){const{login}=useAuth(),nav=useNavigate();const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');async function submit(e){e.preventDefault();try{const u=await login(email,password);const path=u.roles?.includes('SELLER')?'/dashboard/seller':u.roles?.includes('BUYER')?'/dashboard/buyer':u.roles?.includes('INSPECTOR')?'/dashboard/inspector':u.roles?.includes('TRUCK_OWNER')?'/dashboard/truck-owner':'/';nav(path)}catch(e){setError(e.response?.data?.error||'Login failed')}}return <main className="section auth-section"><div className="auth-card card"><span className="eyebrow">WELCOME BACK</span><h1>Log in to MarketBridge.</h1>{error&&<div className="alert error">{error}</div>}<form onSubmit={submit}><label>Email</label><input required type="email" value={email} onChange={e=>setEmail(e.target.value)}/><label>Password</label><input required type="password" value={password} onChange={e=>setPassword(e.target.value)}/><button className="btn btn-primary full" type="submit">Log in</button></form><p className="muted">New to MarketBridge? <Link to="/register">Create an account</Link></p></div></main>}
