import React,{useState}from'react';import{useNavigate,Link}from'react-router-dom';import{useAuth}from'../context/AuthContext.jsx';
const optional=[['INSPECTOR','Independent Inspector'],['TRUCK_OWNER','Truck Owner / Driver'],['ADVERTISER','Advertiser']];
export default function Register(){const{register}=useAuth(),nav=useNavigate();const[form,setForm]=useState({name:'',email:'',phone:'',password:'',location:''}),[selected,setSelected]=useState([]),[error,setError]=useState('');const toggle=r=>setSelected(x=>x.includes(r)?x.filter(a=>a!==r):[...x,r]);async function submit(e){e.preventDefault();try{const u=await register({...form,roles:['BUYER','SELLER',...selected]});nav('/')}catch(e){setError(e.response?.data?.error||'Registration failed')}}return <main className="section auth-section"><div className="auth-card card"><span className="eyebrow">JOIN THE NETWORK</span><h1>One account. Buy and sell.</h1><p className="muted">Every MarketBridge member starts with both buyer and seller capability. Optional specialist roles can be added.</p>{error&&<div className="alert error">{error}</div>}<form onSubmit={submit}><label>Full name</label><input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><label>Email</label><input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><label>Phone</label><input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/><label>Location</label><input value={form.location} onChange={e=>setForm({...form,location:e.target.value})}/><label>Password</label><input required minLength={6} type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><label>Optional capabilities</label><div className="role-select">{optional.map(([r,l])=><button type="button" key={r} className={`role-option ${selected.includes(r)?'selected':''}`} onClick={()=>toggle(r)}>{selected.includes(r)?'✓ ':''}{l}</button>)}</div><button className="btn btn-primary btn-lg full" type="submit">Create account</button></form><p className="muted">Already registered? <Link to="/login">Log in</Link></p></div></main>}
