import React,{useState}from'react';
import{useNavigate,Link}from'react-router-dom';
import api from'../api/client';import{useAuth}from'../context/AuthContext.jsx';
export default function CreateListing(){const{user}=useAuth();const nav=useNavigate();const[category,setCategory]=useState('AGRICULTURAL');const[form,setForm]=useState({sellerId:user?.id||'',title:'',cropType:'',quantity:'',unit:'quintal',askingPrice:'',minAcceptablePrice:'',location:user?.location||'',harvestedDate:'',readinessDate:'',photos:'',videos:''});const[error,setError]=useState('');const set=k=>e=>setForm({...form,[k]:e.target.value});async function submit(e){e.preventDefault();setError('');try{const r=await api.post('/listings',{...form,category,sellerId:user.id,cropType:category==='AGRICULTURAL'?form.cropType:form.title,title:form.title||form.cropType,quantity:Number(form.quantity),askingPrice:Number(form.askingPrice),minAcceptablePrice:form.minAcceptablePrice?Number(form.minAcceptablePrice):undefined,photos:form.photos?form.photos.split(',').map(x=>x.trim()).filter(Boolean):[],videos:form.videos?form.videos.split(',').map(x=>x.trim()).filter(Boolean):[]});nav(`/listings/${r.data.listing.id}`)}catch(e){setError(e.response?.data?.error||'Could not create listing')}}return <main className="section"><div className="container-narrow"><Link to="/" className="back-link">← Home</Link><span className="eyebrow">SELL ON MARKETBRIDGE</span><h1>{category==='AGRICULTURAL'?'List agricultural produce':'List a physical product'}</h1><p className="muted">Your account can buy and sell. Agricultural listings keep the farmer as the price authority.</p><form className="card form-card" onSubmit={submit}>{error&&<div className="alert error">{error}</div>}<label>Marketplace</label><div className="choice-grid"><button type="button" className={`choice ${category==='AGRICULTURAL'?'selected':''}`} onClick={()=>setCategory('AGRICULTURAL')}><b>Agricultural</b><span>Bulk produce, farm lots, inspection and transport workflows.</span></button><button type="button" className={`choice ${category==='PRODUCT'?'selected':''}`} onClick={()=>setCategory('PRODUCT')}><b>Product</b><span>General physical goods sold by MarketBridge members.</span></button></div>{category==='AGRICULTURAL'?<div className="form-grid"><div><label>Produce</label><input required value={form.cropType} onChange={set('cropType')} placeholder="Potatoes, wheat, barley..."/></div><div><label>Quantity</label><input required type="number" min="0.01" value={form.quantity} onChange={set('quantity')}/></div><div><label>Unit</label><select value={form.unit} onChange={set('unit')}><option>quintal</option><option>ton</option><option>kg</option><option>crate</option><option>bag</option></select></div></div>:<div className="form-grid"><div><label>Product title</label><input required value={form.title} onChange={set('title')} placeholder="Product name"/></div><div><label>Quantity</label><input required type="number" min="0.01" value={form.quantity} onChange={set('quantity')}/></div><div><label>Unit</label><input value={form.unit} onChange={set('unit')} placeholder="piece, box, kg..."/></div></div>}<div className="form-grid"><div><label>Asking price (ETB)</label><input required type="number" min="0.01" value={form.askingPrice} onChange={set('askingPrice')}/></div>{category==='AGRICULTURAL'&&<div><label>Minimum acceptable price</label><input type="number" min="0" value={form.minAcceptablePrice} onChange={set('minAcceptablePrice')}/></div>}<div><label>Location</label><input required value={form.location} onChange={set('location')}/></div>{category==='AGRICULTURAL'&&<><div><label>Harvest date</label><input type="date" value={form.harvestedDate} onChange={set('harvestedDate')}/></div><div><label>Ready / pickup date</label><input type="date" value={form.readinessDate} onChange={set('readinessDate')}/></div></>}</div><label>Photo URLs <span className="muted">(comma separated)</span></label><input value={form.photos} onChange={set('photos')} placeholder="https://..."/><label>Video URLs <span className="muted">(comma separated)</span></label><input value={form.videos} onChange={set('videos')} placeholder="https://..."/><button className="btn btn-primary btn-lg full" type="submit">Publish {category==='AGRICULTURAL'?'produce':'product'}</button></form></div></main>}
